% ============================================================================= %
% 狐狸數量變化
% 定義：狐狸數量隨時間增減的速率
% 公式：dy / dt = eta(t - tau) * [beta * x(t - tau) * y(t - tau)] - gamma * y(t)
% ============================================================================= %

function y_rate = rate_fox_num()
end